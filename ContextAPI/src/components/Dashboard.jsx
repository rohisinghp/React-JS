import React from "react";
import Userprofile from "./userProfile";

const Dashboard = (
    // isLoggedIn,
    // handleLogin,
    // handleLogout
) =>{

    return(
        <>

        {/* <Userprofile isLoggedIn={isLoggedIn} handleLogin={handleLogin} handleLogout={handleLogout}/> */}

            <Userprofile/>
        </>
    )
}

export default Dashboard;